#include "ValueConvertor.h"

void
ValueConvertor::CefValueToNSValue(NSObject* oValue, CefValue* cValue) {
  if (!oValue || !cValue) {
    return;
  }

}

void ValueConvertor::NSValueToCefValue(CefValue* cValue, const NSObject* oValue) {
  if (!oValue || !cValue) {
    return;
  }

  
}